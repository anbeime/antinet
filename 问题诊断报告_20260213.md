# Antinet 问题诊断报告

## 问题现状

昨天（2月12日）提交代码后，出现以下问题：
1. ✗ 创建的卡片用不了
2. ✗ Excel功能用不了  
3. ✗ PPT功能用不了

## 根本原因

### 1. 服务未启动
- 后端服务（8000端口）未运行
- 前端服务（5173端口）未运行

### 2. 数据库约束问题
数据库表 `knowledge_cards` 有严格的CHECK约束：
- `card_type` 必须是: 'blue', 'green', 'yellow', 'red'
- `category` 必须是: '事实', '解释', '风险', '行动'

任何不符合这些约束的值都会导致创建/更新失败。

### 3. Excel API路由缺失
后端没有 `/api/excel/health` 路由，说明Excel分析功能的路由可能没有正确注册。

## 当前状态

### ✓ 已修复
- 后端服务已启动（端口8000）
- 数据库完整（98张卡片，数据正常）
- PPT服务正常

### ✗ 待修复
1. 前端服务需要启动
2. Excel功能路由需要检查
3. 卡片创建时需要确保category值符合约束

## 解决方案

### 方案1：回退到昨天提交前的版本（推荐）
```bash
cd C:\test\antinet
git reset --hard HEAD~1
```
这会撤销昨天的提交，恢复到之前的工作状态。

### 方案2：修复当前版本
需要检查并修复以下文件的修改：
1. `backend/routes/knowledge_routes.py` - 确保创建卡片时category有默认值
2. `backend/routes/ppt_routes.py` - 检查PPT处理器的修改
3. `backend/routes/vision_routes.py` - 检查视觉功能的修改
4. 添加Excel路由注册

### 方案3：使用备份数据库（如果数据很重要）
当前数据库有98张卡片，备份只有12张，说明当前数据库是最新的，不需要恢复。

## 建议操作步骤

1. **先备份当前数据库**
   ```bash
   copy backend\data\antinet.db backend\data\antinet.db.backup_20260213
   ```

2. **查看昨天具体改了什么**
   ```bash
   git show 977364e
   ```

3. **决定是回退还是修复**
   - 如果昨天的修改不重要 → 回退（方案1）
   - 如果昨天的修改很重要 → 修复（方案2）

## 快速启动命令

启动后端：
```bash
cd C:\test\antinet
.\venv\Scripts\python.exe backend/main.py
```

启动前端：
```bash
cd C:\test\antinet
pnpm run dev
```

## 数据库约束说明

创建卡片时，必须使用以下值：

**卡片类型 (card_type)**:
- blue（蓝色）
- green（绿色）
- yellow（黄色）
- red（红色）

**分类 (category)**:
- 事实
- 解释
- 风险
- 行动
