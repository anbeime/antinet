# 🚀 Antinet 智能知识管家 - 启动指南

## 📦 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                     前端 (React + TypeScript)            │
│                   http://localhost:3000                   │
│  ┌─────────┐  ┌──────────┐  ┌──────────────────┐         │
│  │  首页   │  │NPU智能分析│  │  NPU 仪表板     │         │
│  │  Home   │  │NPUAnalysis│  │  NPUDashboard    │         │
│  └────┬────┘  └────┬─────┘  └────────┬─────────┘         │
│       │            │                 │                     │
│       └────────────┴─────────────────┘                     │
│                      │ API 请求                            │
└──────────────────────┼─────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│              后端 (FastAPI + Python 3.12 ARM64)          │
│                  http://localhost:8000                    │
│  ┌──────────┐  ┌─────────┐  ┌──────────────────────┐    │
│  │ 锦衣卫   │  │太史阁   │  │   API 接口           │    │
│  │Orchestrator│ │ Memory  │  │   /api/generate/cards│    │
│  └────┬─────┘  └────┬────┘  └──────────────────────┘    │
│       │            │                                       │
│       └────────────┴─────────────┐                        │
│                              │  NPU 推理                 │
│                              ▼                            │
│                   ┌──────────────────┐                    │
│                   │   QNN SDK        │                    │
│                   │  GenieContext    │                    │
│                   └──────────────────┘                    │
│                              │                            │
│                              ▼                            │
│                   ┌──────────────────┐                    │
│                   │  Qwen2.0-7B模型  │                    │
│                   │   NPU 加速       │                    │
│                   └──────────────────┘                    │
└─────────────────────────────────────────────────────────┘
```

## 🎯 快速启动

### 方式1：双窗口启动（推荐）

#### 窗口1 - 启动后端

```powershell
cd c:\test\antinet\data-analysis-iteration
start.bat
```

等待看到：
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

#### 窗口2 - 启动前端

```powershell
cd c:\test\antinet
npm run dev
```

或使用 pnpm：
```powershell
pnpm dev
```

等待看到：
```
  ➜  Local:   http://localhost:3000/
```

### 方式2：单窗口启动（仅启动后端）

如果只需要后端 API（用于测试或 curl）：

```powershell
cd c:\test\antinet\data-analysis-iteration
start.bat
```

## 🌐 访问地址

### 前端页面
- **首页**: http://localhost:3000/
- **NPU 智能分析**: http://localhost:3000/npu-analysis
- **NPU 仪表板**: http://localhost:3000/npu-dashboard

### 后端 API
- **根路径**: http://localhost:8000/
- **API 文档**: http://localhost:8000/docs
- **Swagger UI**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/health

### API 端点
- `POST /api/generate/cards` - 生成四色卡片
- `POST /api/generate/report` - 生成完整报告
- `GET /api/cards` - 获取卡片列表
- `GET /api/knowledge/graph` - 获取知识图谱
- `GET /api/knowledge/search` - 搜索知识

## 🎨 前端功能

### 首页 (`/`)
- ✅ 四色卡片管理系统
  - 🔵 核心概念（蓝色）
  - 🟢 关联链接（绿色）
  - 🟡 参考来源（黄色）
  - 🔴 索引关键词（红色）
- ✅ GTD 系统（Getting Things Done）
- ✅ 团队协作功能
- ✅ 数据分析面板
- ✅ NPU 性能监控
- ✅ 卡片创建、编辑、删除
- ✅ 卡片关联和搜索

### NPU 智能分析 (`/npu-analysis`)
- ✅ 输入查询，通过 8-Agent 协作生成分析结果
- ✅ 实时显示 NPU 推理性能
- ✅ 展示四色卡片结果
- ✅ 性能指标监控

### NPU 仪表板 (`/npu-dashboard`)
- ✅ NPU 性能监控
- ✅ 实时指标展示
- ✅ 可视化图表

## 🧪 测试 API

### 1. 生成四色卡片

```powershell
curl -X POST http://localhost:8000/api/generate/cards `
  -H "Content-Type: application/json" `
  -d "{\"query\": \"分析最近一个月的销售数据趋势\"}"
```

### 2. 生成完整报告

```powershell
curl -X POST http://localhost:8000/api/generate/report `
  -H "Content-Type: application/json" `
  -d "{\"query\": \"分析用户行为趋势\"}"
```

### 3. 获取卡片列表

```powershell
curl http://localhost:8000/api/cards?limit=10
```

### 4. 获取知识图谱

```powershell
curl http://localhost:8000/api/knowledge/graph?limit=100
```

### 5. 搜索知识

```powershell
curl "http://localhost:8000/api/knowledge/search?keyword=销售&limit=10"
```

## 🔧 故障排除

### 问题1：端口被占用

**错误**: `Only one usage of each socket address is normally permitted`

**解决方案**:
```powershell
# 查看占用端口的进程
netstat -ano | findstr :3000
netstat -ano | findstr :8000

# 终止进程
taskkill /F /PID <进程ID>
```

### 问题2：前端无法连接后端

**检查**:
1. 确保后端正在运行（http://localhost:8000）
2. 检查 CORS 配置（`config.py` 中的 `cors_origins`）
3. 查看浏览器控制台的错误信息

### 问题3：NPU 初始化失败

**参考**: `data-analysis-iteration/TROUBLESHOOTING.md`

## 📚 相关文档

- `data-analysis-iteration/8_AGENT_IMPLEMENTATION_COMPLETE.md` - 8-Agent 实现文档
- `data-analysis-iteration/QUICK_START_GUIDE.md` - 快速启动指南
- `data-analysis-iteration/TROUBLESHOOTING.md` - 故障排除指南
- `data-analysis-iteration/references/8-agent-architecture.md` - 8-Agent 架构说明

## 🎉 开始使用

1. **启动后端**: 在窗口1运行 `start.bat`
2. **启动前端**: 在窗口2运行 `npm run dev`
3. **打开浏览器**: 访问 http://localhost:3000
4. **开始使用**: 创建卡片、运行 NPU 分析、探索功能！

---

**享受 Antinet 智能知识管家带来的高效知识管理体验！** 🚀
