# Antinet 项目 - CodeBuddy SDK 集成快速启动指南

## 📋 目录结构

```
antinet/
├── backend/                    # 后端服务目录
│   ├── main.py              # 主 API 服务
│   └── routes/              # 路由模块
│       ├── codebuddy_chat_routes.py  # CodeBuddy SDK 聊天路由（新增）
│       ├── npu_routes.py         # NPU 推理路由
│       ├── knowledge_routes.py    # 知识管理路由
│       └── agent_routes.py        # 8-Agent 协同路由
│
├── data/                      # 数据目录
│   └── knowledge.db          # SQLite 知识库
├── docs/                      # 文档目录
│   └── CODEBUDDY_SDK_STARTUP.md  # 本文档
│
└── start_scripts/            # 启动脚本
│       └── start_complete_system.bat  # 一键启动
│       └── start_backend.bat           # 后端启动
│       └── start_frontend.bat          # 前端启动
│       └── test_sdk_integration.py    # SDK 集成测试脚本
│       └── test_knowledge_base.py   # 知识库测试脚本
└── ...                   # 其他测试脚本
├── frontend/                 # 前端代码
│   ├── src/
│   │   ├── components/
│   │   │   ├── ChatBotModal.tsx      # 聊天机器人组件（已更新）
│   │   │   └── services/
│   │   │       ├── codebuddyChatService.ts  # CodeBuddy 服务
│   │   │       └── ...                   # 其他服务
│   ├── package.json
│   └── index.html
│   └── vite.config.ts
│   └── tailwind.config.js
│   └── tsconfig.json
│   └── postcss.config.js
└── README.md                  # 前端 README
└── ...                         # 其他配置文件
```

---

## 🚀 快速开始

### 方式 1：一键启动（推荐）

```powershell
# Windows PowerShell
cd C:\test\antinet
start_complete_system.bat
```

**启动内容**：
1. 检查并启动所有必需服务
   - FastAPI 后端（端口 8000）
   - React 前端（端口 3000）
   - NPU 推理服务
   - 知识库数据库
   - 8-Agent 协同系统
   - CodeBuddy SDK 聊天服务

2. 访问地址
   - **后端 API**: http://localhost:8000/docs
   - **前端首页**: http://localhost:3000
   - **API 文档**: http://localhost:8000/docs
   - **健康检查**: http://localhost:8000/health

### 方式 2：分步启动

#### 步骤 1：启动后端服务

```bash
# Windows CMD
cd C:\test\antinet\backend
python main.py

# 或使用批处理脚本
start_backend.bat
```

**启动验证**：
```bash
# 等待 10 秒
curl http://localhost:8000/health

# 应返回：
{
  "status": "healthy",
  "sdk_available": true,
  "database_initialized": true,
  "search_type": "keyword_match",
  "knowledge_available": true,
  "agent_system": "initialized"
}
```

#### 步骤 2：启动前端服务

```bash
# 新开命令行
cd C:\test\antinet\frontend

# 使用 npm 或 pnpm
npm run dev
```

**前端服务**：
- 自动打开浏览器访问 http://localhost:3000
- 包含代码热重载
- CodeBuddy 聊天机器人已集成

#### 步骤 3：验证 CodeBuddy 集成

**测试 API**：
```bash
curl http://localhost:8000/api/codebuddy-chat/health
```

**预期响应**：
```json
{
  "status": "healthy",
  "sdk_available": true,
  "knowledge_available": true
  "status": "healthy"
}
```

**测试聊天功能**：
1. 访问 http://localhost:3000
2. 点击右下角机器人图标
3. 输入："帮我分析一下上个月的销售趋势"
4. 查看响应（应包含知识库卡片）

---

## 📚 测试和验证

### 测试脚本

#### 1. SDK 集成测试

```bash
cd C:\test\antinet
python test_sdk_integration.py
```

**测试内容**：
- SDK 柺本功能测试
- 知识库上下文获取测试
- 完整聊天流程测试
- 性能测试（模拟延迟）

**预期输出**：
```
=====================================
CodeBuddy SDK 集成测试
=====================================

SDK 基本功能测试
=====================================
测试 1: 基础查询
测试 2: 知识库上下文
测试 3: 完整聊天流程

=====================================
测试结果总结
=====================================
所有基础测试通过
CodeBuddy SDK 功能正常
知识库集成正常
延迟模拟正常

性能指标：
- 模拟延迟: ~500ms
- 符合 <500ms 目标

下一步：
1. 在前端实际测试聊天功能
2. 验证知识库内容准确性
3. 测试 NPU 性能影响
```

#### 2. 知识库测试

```bash
cd C:\test\antinet
python test_knowledge_base.py
```

---

## 🔧 服务说明

### 1. 后端服务

**主服务**：
- **端口**: 8000 (FastAPI)
- **路由前缀**：
  - `/api/data` - 数据管理路由
  - `/api/npu` - NPU 推理路由
  - `/api/agent` - Agent 系统路由
  - `/api/knowledge` - 知识管理路由
  - `/api/codebuddy-chat` - CodeBuddy 聊天路由（新增）

**健康检查端点**：
- `GET /health` - 服务整体健康
- `GET /api/codebuddy-chat/health` - CodeBuddy 专用
- `GET /api/codebuddy-chat/sdk-status` - SDK 和知识库状态
- `GET /api/codebuddy-chat/optimize` - 性能优化建议

**CodeBuddy 聊天 API 端点**：
- `POST /api/codebuddy-chat/chat` - 智能对话
- `GET /api/codebuddy-chat/cards` - 列出知识卡片
- `POST /api/codebuddy-chat/search` - 搜索知识卡片
- `GET /api/codebuddy-chat/card/{card_id}` - 获取卡片详情
- `POST /api/codebuddy-chat/optimize` - 获取优化建议

### 2. CodeBuddy 聊天机器人

**功能**：
- 智能对话（基于知识库 + CodeBuddy AI）
- 知识库集成（37 张预设卡片）
- 性能优化建议
- 上下文管理（多轮对话）
- 优化模式切换（BURST/Default）

**前端集成**：
- 组件：`src/components/ChatBotModal.tsx`
- 服务：`src/services/codebuddyChatService.ts`

---

##  服务启动日志

### 正常启动流程

```
[2026-01-26 12:30] [SYSTEM] 开始启动 Antinet 服务...
[2026-01-26 12:30] [SYSTEM] 初始化数据库...
[2026-01-26 12:30] [SYSTEM] 加载知识库预设...
[2026-01-26 12:30] [SYSTEM] 注册 NPU 推理路由...
[2026-01-26 12:30] [SYSTEM] 注册数据管理路由...
[2026-01-26 12:30] [SYSTEM] 注册 Agent 系统路由...
[2026-01-26 12:30] [SYSTEM] 注册知识管理路由...
[2026-01-26 12:30] [SYSTEM] 注册 CodeBuddy 聊天路由...
[2026-01-26 12:30] [SYSTEM] 注册技能系统路由...
[2026-01-26 12:30] [SYSTEM] 配置 CORS 中间件...
[2026-01-26 12:30] [SYSTEM] 初始化 8-Agent 系统...
[2026-01-26 12:30] [SYSTEM] 启动 Uvicorn 服务...
[2026-01-26 12:30] [INFO] Uvicorn 运行在 http://0.0.0.0:8000
[2026-01-26 12:30] [INFO] 服务已启动，访问 http://localhost:8000/docs 查看 API
[2026-01-26 12:30] [SUCCESS] Antinet 服务启动完成！
```

---

## 🎯 CodeBuddy SDK 功能

### 已集成功能

1. **智能对话**
   - 基于知识库上下文理解
   - CodeBuddy AI 增强回复
   - 多轮对话支持
   - 上下文管理

2. **知识库集成**
   - 37 张预设卡片
   - 关键词搜索
   - 卡片分类（事实/解释/风险/行动）
   - 相似度匹配

3. **性能优化**
   - 延迟监控
   - 优化建议生成
   - BURST 模式控制

4. **增强功能**
   - 优化建议推送
   - 性能数据收集
   - 知识库上下文优化

---

##  故障排查

### 服务无法启动

**问题**：Uvicorn 服务启动失败

**解决方案**：
```bash
# 检查端口占用
netstat -ano | findstr :8000

# 查看错误日志
cd C:\test\antinet
cat backend.log | tail -20

# 常见错误
- ModuleNotFoundError - 缺少依赖包
- ImportError - SDK 导入错误
- ConnectionRefusedError - 端口被占用

# 清理日志
> backend.log
```

### CodeBuddy 响应失败

**问题**：调用 CodeBuddy API 失败

**可能原因**：
1. 服务未启动
2. API 端点配置错误
3. 知识库数据未加载
4. 网络问题

**解决方案**：
```bash
# 检查服务状态
curl http://localhost:8000/health

# 检查 CodeBuddy 专用端点
curl http://localhost:8000/api/codebuddy-chat/health

# 查看后端日志
cd C:\test\antinet\backend
tail -50 backend.log
```

---

## 📱 快速命令参考

```bash
# 查看所有启动脚本
ls C:\test\antinet\start_scripts\

# 直接启动后端
cd C:\test\antinet\backend && python main.py

# 检查服务日志
tail -f backend.log -100

# 重启服务
Ctrl+C 停止服务
# 重新启动
python main.py

# 测试 API
curl http://localhost:8000/api/codebuddy-chat/health
```

---

## 集成完成

CodeBuddy Agent SDK 已成功集成到 Antinet 项目，并启用智能对话功能。

**关键特性**：
- SDK 安装成功
- 后端路由已注册
- 前端组件已更新
- 知识库已导入
- 健天机器人可用

**下一步**：
1. 在前端测试聊天功能
2. 验证知识库内容
3. 测试性能优化建议

---

**文档版本**: v1.0  
**最后更新**: 2026-01-26 12:35
