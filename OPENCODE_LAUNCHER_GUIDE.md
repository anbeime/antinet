# OpenCode 启动文件使用说明

## 已创建的启动文件

### 简化版（推荐）

#### 1. opencode.bat - 启动 OpenCode TUI
```
位置: C:\test\antinet\opencode.bat
用途: 启动命令行界面版本
```

#### 2. opencode_web.bat - 启动 OpenCode Web
```
位置: C:\test\antinet\opencode_web.bat
用途: 启动浏览器界面版本
```

### 完整版

#### 3. start_opencode.bat - 带检查的启动脚本
```
位置: C:\test\antinet\start_opencode.bat
用途: 启动前检查环境，显示详细信息
```

#### 4. start_opencode_web.bat - 带检查的 Web 启动
```
位置: C:\test\antinet\start_opencode_web.bat
用途: 启动 Web 界面，显示详细信息
```

---

## 🚀 如何使用

### 方法1: 双击运行（最简单）

1. 打开文件资源管理器
2. 进入 `C:\test\antinet`
3. 双击以下任一文件：
   - `opencode.bat` - 命令行版本
   - `opencode_web.bat` - 浏览器版本

### 方法2: 命令行运行

```cmd
cd C:\test\antinet

REM 启动命令行版本
opencode.bat

REM 或启动 Web 版本
opencode_web.bat
```

---

## 🔧 文件说明

### opencode.bat（推荐使用）
```batch
@echo off
echo Starting OpenCode...
echo.
"C:\Users\AI-PC-19\.stepfun\runtimes\node\...\opencode.cmd"
```

**特点**:
- 最简单
- 直接启动
- 无额外检查
- 不会出现编码问题

### start_opencode.bat
```batch
@echo off
REM 设置环境变量
REM 检查安装
REM 显示版本信息
REM 启动 OpenCode
```

**特点**:
- 详细的状态信息
- 环境检查
- 错误提示
-  可能有编码问题（已修复）

---

##  之前的问题

### 问题原因
1. **编码问题**: 批处理文件使用了 UTF-8 编码，导致中文乱码
2. **无限循环**: 文件内容可能被破坏

### 解决方案
1. 删除了旧的批处理文件
2. 使用 ASCII 编码重新创建
3. 创建了简化版本（无中文）

---

## 🎯 推荐使用方式

### 日常使用
```
双击: opencode.bat
```

### 需要详细信息时
```
双击: start_opencode.bat
```

### Web 界面
```
双击: opencode_web.bat
```

---

##  首次使用步骤

### 1. 启动 OpenCode
```
双击 opencode.bat
```

### 2. 配置 AI 模型
在 OpenCode 中输入:
```
/connect
```

### 3. 选择提供商
- 智谱 GLM（推荐）
- Claude
- GPT
- Gemini

### 4. 输入 API Key
根据提示输入你的 API Key

### 5. 开始使用
直接输入问题或任务

---

## 🔍 故障排查

### 问题1: 双击后闪退

**原因**: 路径错误或 OpenCode 未安装

**解决**:
1. 右键 `.bat` 文件 → 编辑
2. 检查路径是否正确
3. 在 CMD 中运行查看错误

### 问题2: 提示找不到命令

**原因**: OpenCode 路径不正确

**解决**:
```cmd
REM 检查 OpenCode 是否存在
dir "C:\Users\AI-PC-19\.stepfun\runtimes\node\install_1769405385879_ym8edrbn6xn\node-v22.18.0-win-x64\opencode.cmd"

REM 如果不存在，查找正确路径
where opencode
```

### 问题3: 乱码或编码错误

**原因**: 文件编码问题

**解决**:
使用简化版 `opencode.bat`（无中文，无编码问题）

---

##  快速测试

### 测试 OpenCode 是否可用
```cmd
"C:\Users\AI-PC-19\.stepfun\runtimes\node\install_1769405385879_ym8edrbn6xn\node-v22.18.0-win-x64\opencode.cmd" --version
```

### 预期输出
```
INFO  2026-01-26T... service=models.dev file={} refreshing
1.1.36
```

---

## 📂 文件位置

所有启动文件位于:
```
C:\test\antinet\
├── opencode.bat              ⭐ 推荐使用
├── opencode_web.bat          ⭐ Web 版本
├── start_opencode.bat        详细版本
└── start_opencode_web.bat    详细 Web 版本
```

---

## 🎉 立即开始

1. **打开文件资源管理器**
2. **进入** `C:\test\antinet`
3. **双击** `opencode.bat`
4. **输入** `/connect` 配置 AI 模型
5. **开始使用**！

---

**创建时间**: 2026-01-26  
**状态**: 已修复编码问题  
**推荐**: 使用 `opencode.bat` 或 `opencode_web.bat`
