# 🚀 快速测试指南

## ✅ 当前状态

**后端已运行**：http://localhost:8000 ✅
- API 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/api/health

**前端问题**：Node.js 未安装 ❌

---

## 🧪 立即测试后端（无需安装）

### 方式 1：使用测试页面（最简单）

**直接双击**：`c:\test\antinet\test.html`

或者：
1. 打开浏览器
2. 按 Ctrl+O
3. 选择 `c:\test\antinet\test.html`

**测试功能**：
- ✅ 检查后端健康状态
- ✅ 查看可用模型
- ✅ 执行 NPU 推理（生成四色卡片）
- ✅ 运行性能基准测试

### 方式 2：使用 API 文档

**访问**：http://localhost:8000/docs

在 Swagger UI 中：
1. 展开 `/api/health` - 点击 "Try it out" - "Execute"
2. 展开 `/api/npu/analyze` - 输入查询 - 点击 "Execute"

---

## 🔧 修复环境（可选）

### 问题 1：pydantic-core 损坏

**在 PowerShell 中执行**：
```powershell
# 删除损坏的包
pip uninstall -y pydantic-core pydantic

# 重新安装
pip install pydantic-core==2.14.6 pydantic==2.5.3
```

**如果提示权限错误**：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 问题 2：Node.js 未安装

**步骤 1**：下载 Node.js
- 访问：https://nodejs.org
- 下载 LTS 版本（推荐 20.x）
- 安装（一路下一步）

**步骤 2**：验证安装
```powershell
node --version
npm --version
```

**步骤 3**：安装前端依赖
```powershell
cd c:\test\antinet
npm install
```

**步骤 4**：启动前端
```powershell
npm run dev
```

然后访问：http://localhost:3000

---

## 📱 骁龙 AIPC 操作

**操作完全相同**：

1. **拉取代码**：
   ```powershell
   git clone https://github.com/anbeime/antinet.git
   cd antinet
   ```

2. **测试后端**：
   - 双击 `test.html`
   - 或访问 http://localhost:8000/docs

3. **安装 Node.js**（如果未预装）：
   - 从 https://nodejs.org 下载
   - 运行 `npm install`
   - 运行 `npm run dev`

---

## 🎯 建议优先级

### 高优先级（必须）
1. ✅ 双击 `test.html` 测试后端功能
2. ✅ 测试 NPU 推理是否正常
3. ✅ 验证四色卡片生成

### 中优先级（增强体验）
4. 安装 Node.js
5. 安装前端依赖（npm install）
6. 启动完整前端（npm run dev）

---

## 📊 性能预期

| 功能 | 预期时间 | 说明 |
|------|----------|------|
| 后端启动 | < 3s | FastAPI 服务 |
| 模型加载 | 5-10s | 首次推理 |
| NPU 推理 | < 500ms | 目标性能 |
| 四色卡片 | < 1s | 包含推理时间 |

---

## 💡 快速开始

**现在就可以开始**：

1. 打开 `test.html`
2. 点击"检查后端健康状态"
3. 点击"执行 NPU 推理"
4. 查看生成的四色卡片

---

## ❓ 常见问题

### Q: test.html 打开后什么都没有？
A: 等待几秒，页面会自动检查健康状态

### Q: 推理失败怎么办？
A:
- 检查后端是否运行：http://localhost:8000/api/health
- 查看错误信息
- 查看后端日志：`C:\test\antinet\backend.log`

### Q: 前端端口 3000 无法访问？
A:
- Node.js 未安装
- 前端未启动（npm run dev）
- 使用 test.html 作为替代方案

---

**总结**：
- ✅ 后端已就绪，可以测试
- ✅ 使用 test.html 无需安装任何东西
- ⚠️ 完整前端 UI 需要安装 Node.js

**立即开始测试**：双击 `test.html` 🚀
