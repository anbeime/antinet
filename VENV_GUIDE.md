# 🔧 Antinet 虚拟环境使用指南

## 📋 当前虚拟环境状态

您的项目已经配置了虚拟环境，所有部署脚本已更新以支持虚拟环境。

### 已检测到的虚拟环境

| 虚拟环境 | Python版本 | 架构 | 状态 |
|---------|-----------|------|------|
| `venv_arm64` | 3.12.10 | ARM64 | 已配置 |

### 已安装的依赖

在 `venv_arm64` 虚拟环境中：
- FastAPI 已安装
- QAI AppBuilder 已安装
- 所有后端依赖已就绪

---

## 🚀 快速开始（使用虚拟环境）

### 方式1: 使用快速启动脚本（推荐）

```cmd
cd C:\test\antinet
start_all.bat
```

脚本会自动：
1. 检测并使用虚拟环境
2. 配置 NPU 环境
3. 启动后端服务（使用虚拟环境）
4. 启动前端服务
5. 自动打开浏览器

### 方式2: 手动启动（使用虚拟环境）

```cmd
# 1. 进入项目目录
cd C:\test\antinet

# 2. 激活虚拟环境
venv_arm64\Scripts\activate

# 3. 配置 NPU 环境
call set_env.bat

# 4. 启动后端（新窗口）
cd backend
python main.py

# 5. 启动前端（新窗口，另一个终端）
cd C:\test\antinet
pnpm run dev
```

---

## 📦 部署脚本更新

所有部署脚本已更新，现在会自动检测并使用虚拟环境：

### deploy_antinet.bat
- 自动检测 `venv_arm64` 或 `venv`
- 优先使用虚拟环境中的 Python
- 在虚拟环境中安装依赖
- 显示使用的虚拟环境信息

### start_all.bat
- 检查虚拟环境是否存在
- 使用虚拟环境中的 Python 启动后端
- 显示虚拟环境路径

### health_check.bat
- 检查虚拟环境状态
- 验证虚拟环境中的依赖
- 显示虚拟环境信息

### stop_all.bat
- 停止所有服务（无需修改）

---

## 🎯 虚拟环境优势

### 为什么使用虚拟环境？

1. **隔离性** - 项目依赖独立，不影响系统 Python
2. **版本控制** - 可以使用特定版本的 Python（3.12.10）
3. **依赖管理** - 清晰的依赖列表，易于管理
4. **可移植性** - 便于在不同环境中部署

### 虚拟环境 vs 系统 Python

| 特性 | 虚拟环境 (venv_arm64) | 系统 Python |
|------|---------------------|------------|
| Python 版本 | 3.12.10 | 3.11.9 |
| 依赖隔离 | 是 | ❌ 否 |
| 推荐使用 | 是 |  备选 |

---

##  虚拟环境管理

### 激活虚拟环境

```cmd
# 在项目根目录
cd C:\test\antinet

# 激活虚拟环境
venv_arm64\Scripts\activate

# 激活后，命令提示符会显示 (venv_arm64)
```

### 退出虚拟环境

```cmd
# 在激活的虚拟环境中
deactivate
```

### 查看已安装的包

```cmd
# 激活虚拟环境后
pip list

# 或直接使用虚拟环境的 pip
venv_arm64\Scripts\pip list
```

### 安装新的依赖

```cmd
# 激活虚拟环境后
pip install <package_name>

# 或直接使用虚拟环境的 pip
venv_arm64\Scripts\pip install <package_name>
```

### 更新依赖

```cmd
# 激活虚拟环境后
pip install --upgrade <package_name>

# 更新所有依赖
pip install --upgrade -r backend\requirements.txt
```

---

## 🔍 验证虚拟环境

### 检查虚拟环境状态

```cmd
cd C:\test\antinet
health_check.bat
```

输出示例：
```
[1/8] Environment Check
----------------------------------------
  [OK] System Python 3.11.9
  [OK] Virtual Environment Python 3.12.10 (ARM64)
  [OK] Node.js v22.18.0
  [OK] pnpm 10.28.0

[2/8] Virtual Environment Check
----------------------------------------
  [OK] venv_arm64 directory exists
  [OK] Activation script exists

[3/8] Dependencies Check
----------------------------------------
  [OK] Backend dependencies installed in virtual environment
  [OK] QAI AppBuilder installed in virtual environment
  [OK] Frontend dependencies installed
```

### 手动验证

```cmd
# 检查虚拟环境 Python 版本
venv_arm64\Scripts\python --version

# 检查 FastAPI
venv_arm64\Scripts\python -c "import fastapi; print('FastAPI OK')"

# 检查 QAI AppBuilder
venv_arm64\Scripts\python -c "import qai_appbuilder; print('QAI AppBuilder OK')"
```

---

## 🐛 常见问题

### Q1: 虚拟环境未激活，脚本能正常运行吗？

**A:** 可以！更新后的脚本会自动使用虚拟环境中的 Python，无需手动激活。

```cmd
# 直接运行即可，脚本会自动使用 venv_arm64
start_all.bat
```

### Q2: 如何切换到系统 Python？

**A:** 如果需要使用系统 Python，可以临时重命名虚拟环境目录：

```cmd
# 重命名虚拟环境（临时禁用）
ren venv_arm64 venv_arm64_backup

# 运行脚本（会使用系统 Python）
start_all.bat

# 恢复虚拟环境
ren venv_arm64_backup venv_arm64
```

### Q3: 虚拟环境损坏了怎么办？

**A:** 删除并重新创建：

```cmd
# 1. 删除旧的虚拟环境
rmdir /s /q venv_arm64

# 2. 创建新的虚拟环境
python -m venv venv_arm64

# 3. 激活虚拟环境
venv_arm64\Scripts\activate

# 4. 安装依赖
pip install -r backend\requirements.txt
pip install "C:\test\qai_appbuilder-2.38.0-cp312-cp312-win_arm64.whl"
```

### Q4: 如何创建新的虚拟环境？

**A:** 使用 Python 的 venv 模块：

```cmd
# 创建 ARM64 虚拟环境
python -m venv venv_arm64

# 或创建 x64 虚拟环境
python -m venv venv

# 激活并安装依赖
venv_arm64\Scripts\activate
pip install -r backend\requirements.txt
```

---

## 📊 虚拟环境目录结构

```
C:\test\antinet\
├── venv_arm64\              # ARM64 虚拟环境
│   ├── Scripts\             # 可执行文件
│   │   ├── python.exe       # Python 解释器
│   │   ├── pip.exe          # 包管理器
│   │   ├── activate.bat     # 激活脚本
│   │   └── deactivate.bat   # 退出脚本
│   ├── Lib\                 # Python 库
│   │   └── site-packages\   # 已安装的包
│   └── pyvenv.cfg           # 虚拟环境配置
├── backend\                 # 后端代码
├── node_modules\            # 前端依赖
└── ...
```

---

## 🎯 最佳实践

### 推荐做法

1. **始终使用虚拟环境** - 保持项目依赖隔离
2. **使用快速启动脚本** - `start_all.bat` 会自动处理虚拟环境
3. **定期更新依赖** - 保持依赖包最新
4. **备份虚拟环境** - 重要项目建议备份 `requirements.txt`

### ❌ 不推荐做法

1. **不要混用系统 Python 和虚拟环境** - 可能导致依赖冲突
2. **不要手动修改虚拟环境文件** - 使用 pip 管理依赖
3. **不要提交虚拟环境到 Git** - 虚拟环境应该被 `.gitignore` 忽略

---

## 🔄 迁移到虚拟环境

如果您之前使用系统 Python，现在想迁移到虚拟环境：

```cmd
# 1. 创建虚拟环境
python -m venv venv_arm64

# 2. 激活虚拟环境
venv_arm64\Scripts\activate

# 3. 安装依赖
pip install -r backend\requirements.txt

# 4. 安装 QAI AppBuilder
pip install "C:\test\qai_appbuilder-2.38.0-cp312-cp312-win_arm64.whl"

# 5. 验证安装
python -c "import fastapi, qai_appbuilder; print('All OK')"

# 6. 使用快速启动脚本
deactivate
start_all.bat
```

---

## 📚 相关文档

- [QUICK_START.md](./QUICK_START.md) - 快速开始指南
- [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) - 详细部署指南
- [DEPLOYMENT_COMPLETE.md](./DEPLOYMENT_COMPLETE.md) - 部署完成总结

---

## 🎉 总结

### 当前状态
- 虚拟环境 `venv_arm64` 已配置
- Python 3.12.10 (ARM64)
- 所有依赖已安装
- 部署脚本已更新支持虚拟环境

### 快速开始
```cmd
cd C:\test\antinet
start_all.bat
```

### 服务地址
- **前端**: http://localhost:3000
- **后端**: http://localhost:8000

**虚拟环境已就绪，随时可以开始使用！** 🚀

---

*文档更新时间: 2026-01-26*  
*虚拟环境: venv_arm64*  
*Python 版本: 3.12.10*
