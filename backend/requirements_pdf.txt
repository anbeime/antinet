# PDF 处理相关依赖（ARM Windows 兼容版本）
# ========================================
# 安装方法: pip install -r requirements_pdf.txt

# ===== 基础PDF处理（纯Python，无系统依赖）====
pypdf>=3.17.0          # PDF 基础操作（文本提取）
pdfplumber>=0.10.0     # PDF 文本和表格提取
reportlab>=4.0.0       # PDF 生成
Pillow>=10.0.0           # 图像处理支持

# ===== 高级功能（需要系统工具，仅x64）=====
# 以下功能仅在x64 Windows上可用，ARM64会自动跳过
# pymupdf>=1.23.0        # 图片提取（x64 only）
# pytesseract>=0.3.10   # OCR识别（需要Tesseract+x64）
# pdf2image>=1.17.0      # PDF转图片（需要poppler+x64）
# camelot-py[cv2]>=0.11.0 # 表格提取（需要Ghostscript+x64）
